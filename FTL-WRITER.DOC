# FTL-WRITER DOCUMENTATION

FTL-WRITER is a simple word processor for MacOS, Linux, and classic 68k Amiga (KS 2.0+). Inspired by old DOS word processors like WordStar and WordPerfect, its main intent is to be used by writers of prose, whether that's fiction/non-fiction, poetry, or articles. As such it sticks to the very basics. You can set headings, mark text as bold, underline, or italic, or horizontal lines.
Documents are saved in Markdown format. Technically you can use any Markdown formatting marks in your document and they will be saved, but they won't be parsed and displayed in FTL-WRITER if you have Markdown=on.
And in line with that, documents converted from other formats will lose things like images or table structures. FTL-WRITER will keep all text, but will ignore structure that isn't bold, italic, underline, headings, and lists.
FTL-WRITER's goal is to let you write without distractions and without having to fight your word processor.

## 1.0 - FEATURES

Pressing F1 will pull down a menu showing you the options, including key shortcuts, and configuration settings. Modifying these settings from their defaults will save a config file in your home directory called ~/.ftl-writer which is easily editable by you and is in a simple key = value format.
The Function keys should be fairly straight forward to understand, Open opens a file, Save saves the current file, New Document clears the current document and starts a fresh one.

### 1.1 - BASIC EDITING

^O - Open Document

^S - Save current document

^Q - Quit program

^Y - Delete screen line (not the whole paragraph to a newline)

^B - Page Up (think of it as "Back")

^F - Page Down (think of it as "Forward")

^U - Half Page Up

^D - Half Page Down

^T - Top of document

Shift-Home - Top of document

^Z - Bottom of document

Shift-End - Bottom of document

Shift-left/right - Word left/right

Shift-up/down - Paragraph up/down

^A - Start of screen line

^E - End of screen line

^K - Block commands (see section 1.3 - BLOCKS for details)

^K - Starts a new block selection if none is currently active

^Kc - copy selected block

^Kx - cut selected block

^Kb - make selected block **Bold**

^Ki - make selected block *Italic*

^Ku - make selected block <u>Underline</u>

^K1-4 - make first line of selection a header (H1 - H4)


### 1.2 - FILE BROWSER

F5 View Files opens a file browser allowing you to browse for and open documents.
The browser will start in your configured Doc Dir, but will remember where you are each session. Next time you load FTL-WRITER, it will start you back in your configured Doc Dir.
Navigate using left/right arrow keys, or pressing Enter to select a folder or a document. Left arrow key will always go to the parent directory, regardless where you are when browsing. 
Right arrow will only go forward a directory if you have that directory highlighted.

Editor settings that affect the File Browser are:
-Hidden Dirs
-Doc Dir
-Extensions

### 1.3 - BLOCKS

Blocks are "marked" by pressing Ctrl-K at the point you want to start your block, moving to the last character and pressing Ctrl-K again. From here you can press B to make that selection bold, U for underline, I for italic, C to copy, or X to cut. If you have Markdown on in your settings, these will display appropriately.
Pressing 1-4 will change the first line of that block in a heading of that depth. In Markdown, these are prepended with # characters. A single # is considered an H1 or first level header. 2, a 2nd level, and so on. Because there's no good way of showing these in different font sizes in a terminal display, they are left as is for display purposes. But if you were to load the document in a graphical Markdown reader, then they would display appropriately proportioned.
When a block is selected, you may also choose to Copy (Ctrl-C) or Cut (Ctrl-X). Then move the cursor to where you want that pasted, and press Ctrl-V to Paste it in.

## 2.0 - PRINTING

Document will be converted to PDF in memory and sent to your print queue. Print settings can be adjusted by pressing Shift-F9.
Printing is set exclusively to the Courier font. 

## 3.0 - CONFIG FILE OPTIONS

There are several options that can be placed in the user config file, ~/.ftl-writer.
Some options configure the editor (accessible from the F1 menu under SETTINGS) and some configure the print output (accessible from Shift-F9 which will bring up its own settings panel).

### 3.1 - EDITOR SETTINGS

A few other settings you can adjust are setting tab width, showing a wordcount, automatically indenting paragraphs, backups, and auto saving. These are specific settings to the editor screen of your word processor.

<u>Auto Save</u> defaults to off (0 minutes), but can be set to any number of minutes you choose. Current document will automatically save at those intervals.
Config file key: **editor.autosave =** *number*

<u>Backups</u> defaults to off. If you set it on, each time you save the document, the previous one will be moved to <filename>.BAK.
Config file key: **editor.backups =** *on/off* 

<u>Doc Dir</u> is your starting document directory. This will be where FTL-WRITER starts looking and saving files to by default each time you load it. 
Config file key: **editor.doc_dir =** *directory*

<u>Extensions</u> defaults to .md,.docx,.odt. These are the files that are shown in the file browser and can be configured to whatever extensions you want, so that you can focus on just seeing the documents you want.
The dot prefix is optional, and extensions are case insensitive, so .doc will also show files ending in .DOC, .Doc, etc. If you don't include the dot prefix, FTL-WRITER will always assume it's there, so md,doc would be interpreted as .md,.doc.
This can be any comma separated list of extensions, including * to list all files. Note, this setting only applies to files, not directory names. 
Config file key: **editor.extensions =** *extensions*

<u>Hidden Dirs</u> defaults to on. This shows directories starting with dot "." in the file browser. Usually these are configuration directories, so some folk may want to turn that off.
Config file key: **editor.dotdirs =** *on/off*

<u>Indent</u> defaults to on. This displays an indent at the start of every paragraph. Note, this is only for display purposes and is separate to Markdown on/off. Paragraphs will be saved without the indents in the document file as indented lines in Markdown are considered "code" and will display in a monospace font with graphical Markdown viewers. 
Also note that paragraphs are saved with blank lines between them as per the Markdown spec, but they just don't display as such on the screen. On screen, it's designed to look like a book. 
Config file key: **editor.indent =** *on/off*

<u>Markdown</u> defaults to on. This will show bold, italic, and underlines. Turning it off will just show the raw Markdown codes.
Config file key: **editor.markdown =** *on/off*

<u>Tabs</u> defaults to 4 and is the number of characters shifted when you press the Tab key.
Config file key: **editor.tabs =** *on/off*

<u>Wordcount</u> defaults to on. This shows a wordcount in the top title bar. Turning it off will have the results you probably expect.
Config file key: **editor.wordcount =** *on/off*

<u>Wrap</u> defaults to on. This will wrap words at the screen boundary. Turning it off will display literally one line of text up to a newline character as one single line on the screen. Turning it off will make it difficult to read and edit because terminals don't have horizontal scroll bars, but you do you.
Config file key: **editor.wrap =** *on/off*

### 3.2 - PRINT SETTINGS

These settings are specific to the printed output.

<u>Base Size</u> is the base font size when the document is printed. This defaults to 12. Headings will adjust accordingly.
Config file key: **print.basesize =** *number*

<u>Double Spacing</u> allows you to set vertical spacing blanks. So one line of text, one blank line. Default is off.
Config file key: **print.doublespace =** *on/off*

<u>Indent</u> replicates the effect in the editor of having the first line of each paragraph indented. Defaults to on.
Config file key: **print.indent =** *on/off*

<u>Paragraph Spacing</u> allows a blank line to be inserted after every paragraph. Defaults to off.
Config file key: **print.paraspace =** *on/off*

### 3.3 - EXAMPLE CONFIG FILE

An example config file looks like this:

    editor.doc_dir = ~/Documents
    editor.extensions = *
    editor.wrap = on
    editor.autosave = 0
    editor.backups = off
    editor.tabs = 4
    editor.wordcount = on
    editor.indent = on
    editor.markdown = on
    editor.dotdirs = on
    print.basesize = 12
    print.doublespace = off
    print.indent = on
    print.paraspace = off

There's no set order, and it's not necessary to include all of them. If you're happy with the defaults, then there's no need to include that in the config file.

## 4.0 - CONVERSIONS

It's possible to load MS Word .docx files, legacy MS Word .doc files, OpenDocument Format .odt files, and WordStar 4 documents. 
When these are detected, you will be asked to convert it and if you say Yes, then these will be converted to Markdown and a copy saved as a .md file.
Note though that .doc is a generic file extension and not a specific format, so FTL-WRITER will determine what the file actually is and convert if necessary, otherwise just display as-is. 



---
FTL-WRITER is Copyright (c) 2026 by Spiro Harvey
It is Buy-Me-A-Coffee-Ware. You may use it freely and distribute it, but may not profit from it or include it in a commercial bundle or application without express written consent from Spiro Harvey. 
If you like it, enjoy it, and are able to offer financial support, then please donate at https://buymeacoffee.com/spiroharvey

